[pytest]
# Run from the backend/ directory: `cd backend && python -m pytest`
# The presence of this ini file makes backend/ the rootdir, so `tests/`
# is resolved relative to it and the package imports resolve correctly.
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q
