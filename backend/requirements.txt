# Pinned to the versions verified on the dev box (Python 3.12–3.14, pure-Python).
fastapi==0.137.1
uvicorn[standard]==0.49.0
networkx==3.6.1
numpy==2.4.6
pydantic==2.13.4
httpx==0.28.1        # for TestClient / smoke tests
